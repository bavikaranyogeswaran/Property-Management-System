import { vi } from 'vitest';

// Global mocks can be placed here
// e.g., mocking the database or Redis client if needed

// Example:
// vi.mock('../config/db', () => ({
//   default: {
//     query: vi.fn(),
//   },
// }));

beforeAll(() => {
  // setup before all tests
});

afterAll(() => {
  // teardown after all tests
});
